# 私语手札 0.24.0

以用户提供的 `My-Dear-Memory-main v9.8.zip`（内部版本 0.23.4）为基础。

## 本次修改

1. 同一故事日、同一栏目合为一页。模型把多段拆成多页时，会在保存前合并；手动再次生成和跨日自动整理会补充到对应的原页。新一天独立记录，跨阶段仍覆盖整个范围。单换行正确分段，较长无换行的中文正文只在句末增加段落，不删减结尾。
2. 保留“印象”栏目和初印象，移除其中的整体、气质、性格、心动、自定义角度 Tab 及相关生成参数。
3. API 设置位置：**酒馆扩展程序 → 私语手札 → API 设置**。主/副 API、连接配置、模型列表和手动模型 ID 都在这里操作；日记本内不再显示设置按钮。
4. 贺卡、情书增加封信动画：空白纸面浮现文字 → 翻转展示花纹背面 → 打开信封 → 信纸滑入 → 封口。五种信纸与信封随手札封面切换，使用提供的五张参考图。可先预览，保存预约或成功手动寄送后也会播放；跳过动画不取消寄信。动画纸面是展示预览，全文保存在信箱。
5. 压缩现有封面、桌面和动画图片。默认系统字体离线可用，英文花体继续内置，中文在线字体仍为选择后按需加载。没有新增视频、GIF、动画库或全量中文字体。

## 更新方法

解压后，用 `My-Dear-Memory-main` 文件夹内的文件覆盖酒馆现有插件文件夹，保持原安装目录不变。刷新页面，底部运行版本应显示 `v0.24.0`。无需删除扩展或清空浏览器数据。

已有且能明确识别同一故事日/同一阶段的同栏条目会在读取时合并，保留各段文字和本地装饰；无法确认日期的旧条目保留独立，避免按现实保存时间误合并。仍可使用“备份 JSON”导出手札。

## 保留功能与验证范围

- 截断检测、最多两次续接、保留不完整响应与原段落边界。
- 导入已保存情书、预约、取消、立即寄出、跨故事日期补寄、聊天消息去重和保存失败重试；寄信不调用模型。
- 沿用上传版本的聊天显示方式：可见区显示简短寄送提示，信件原文放在隐藏 Request 注释中供下一轮角色回应。
- 使用本地回归脚本和真实 Chromium 浏览器配合酒馆接口模拟检查；手机与桌面宽度为 390px / 1280px。真实酒馆环境、实际模型输出质量和 iPhone Safari 仍需安装后验收。

开发测试留在工作目录，安装 ZIP 仅包含运行文件和说明。
