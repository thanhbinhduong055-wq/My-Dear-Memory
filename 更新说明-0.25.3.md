# 0.25.3：跨日识别与手动按日期整理

## 自动整理

- 新增无“日期”标签的时间栏识别，例如 `【2025年11月7日 09:30 星期五|办公室|晴】`，与带标签日期按出现顺序比较，避免较早的 User 日期压过后面的 Char 时间栏。
- 首次读取的回复内部若由前一天推进到次日，先从首个日期建立基准，再检查末尾日期，不再直接将末尾日期当作初始日期。
- 保留“第二天”等相对时间识别与思维链排除。
- 在底部明确显示等待正文、等待手札、日期倒退等状态，方便定位真实环境中的剩余问题。

## 手动整理

翻开手札，在底部点击 **按日期一键整理**。选择开始、结束故事日期；单日选择同一天。点击 **一次生成四个板块**。

- 使用当前选择的正文 API 或副 API，一次模型请求；不自动续写追加请求。
- 生成印象、相处日记、情书，伴侣关系成立时同时生成恋爱日记。
- 起止日期均包含，读取当前聊天中对应日期的可见正文；同一回复里的跨日段落分开处理，不读取现实消息时间和思维链。
- 同日多次整理继续沿用按日合并逻辑；相同时间段且正文未变化时阻止重复请求。
- 找不到对应剧情则提示调整日期；超过 5 万字则提示缩小范围，不默默截取所选正文。
- 响应缺栏或截断会保留收到的文字并提示，可再次手动尝试；模型输出完整性不能由客户端保证。

## 验证边界

新增回归用例先在 0.25.2 复现两项失败，再在 0.25.3 验证通过。测试覆盖日期识别、13个调度场景、范围选择与过滤、保存、请求次数、正文/副API、重复点击、空范围及截断。浏览器测试使用 Chrome 390px/1280px 和模拟酒馆事件、存储及模型响应，不消耗真实模型额度；不等同于真实 iPhone 或用户云酒馆验证。
