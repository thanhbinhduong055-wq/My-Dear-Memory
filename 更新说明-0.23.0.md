# 私语手札 0.23.0 修正版

## 更新后从哪里使用

- **日历信箱**：打开手札后，点右侧信封书签；心情月历上方也有“打开日历信箱”。选择故事日期，写贺卡 / 情书，或从已保存的情书填入，然后预约或立即寄出。信箱显示全部日期的信件、原文及待寄出 / 已寄出 / 已取消状态；待寄信件可直接取消。
- **进入正文**：故事到达或跨过预约日期、本轮正文结束后，信件原文作为 User 消息加入当前聊天并保存。下一轮角色回复可读到信件。寄出不会主动调用模型。首次识别需要明确年月日；之后可识别“第二天、两天后”等。关闭酒馆期间不运行，重新打开聊天后会补查跨过的日期。
- **字体**：翻开手札，点击右上角“字体”。可选宋体、中文行书、英文花体、系统黑体；预览立即变化，选择自动记忆。正文、编辑信件及已保存的信件使用所选字体；按钮和操作标签保持易读。三款字体随包附带，不依赖外部字体服务；英文花体中的中文字使用宋体，缺字由系统字体补足。

## 本次修复

1. 手动输出上限从 1800 提高到 5200 token；普通批量维持 10000，跨阶段批量维持 24000。上限不是实际消耗量。
2. 发现标签 / JSON 输出截断后，最多追加两次请求续接原文，完整响应仍只请求一次。补全失败或切换聊天时保留已收到的内容，并标注尚未写完；不会把残文标成完整。续接保留段落、英文空格和已有正文，避免同一页重复保存。
3. 修复宽松 JSON 恢复时，把正文中的引号和逗号误当结尾的问题。
4. 修复隐藏栏目控件后，正文没有占据可滚动网格行的问题；相处日记、情书与信箱也可以滚动查看结尾。
5. 增加独立信箱入口和月历快捷入口，显示所有日期的信件。修复关闭聊天期间多个相对日期只累计最后一次的问题；宿主忙碌时延后检查，并补上消息落地触发检查。

## 安装这个 ZIP

先在旧版里备份 JSON。解压后，将 `My-Dear-Memory-main` 内的文件覆盖到**原来的扩展目录**，包含 `index.js`、`style.css`、`manifest.json` 和 `assets/fonts`。不要并排安装两个相同插件。

刷新酒馆后，确认手札底部显示 **v0.23.0**。若仍显示旧版本，请强制刷新 / 清理该站点缓存。保留原扩展目录和原聊天即可继续读取原手札数据；原 ZIP 不被修改。

旧页面没有收到的结尾无法凭空还原；可以编辑旧页，或重新生成。模型、服务商的硬性输出限制或网络故障仍可能导致两次补全后未完成，此时会明确标注。

## 验证范围

提供 `tests/smoke.cjs`、`tests/mail.cjs`、`tests/revision.cjs` 和 `tests/mail-browser.cjs`。浏览器测试使用 SillyTavern 接口替身，检查移动 / 桌面布局、实际字体加载、截断续接、预约、导入、立即寄出、取消与防重寄；不访问用户模型账号。

用户实际酒馆版本、移动端 Safari / 云酒馆及真实模型的最终验收仍需在安装后确认。

## 字体来源与许可

字体均随附 SIL Open Font License 1.1 原文，位于 `assets/fonts/*-OFL.txt`。

- [Great Vibes](https://github.com/google/fonts/tree/main/ofl/greatvibes)：英文花体。
- [Zhi Mang Xing](https://github.com/google/fonts/tree/main/ofl/zhimangxing)：中文志莽行书。
- [Noto Serif SC](https://github.com/google/fonts/tree/main/ofl/notoserifsc)：中文宋体。

寄信所用的 `chat`、`addOneMessage`、`saveChat` 接口与 [SillyTavern 官方上下文导出](https://github.com/SillyTavern/SillyTavern/blob/release/public/scripts/st-context.js)核对。
