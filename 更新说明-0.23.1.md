# 0.23.1 精简修正版

本版只优化资源体积，保留 0.23.0 的手札截断补全、日历信箱、字体选择与主题功能。

- 三款内置字体改用 WOFF2。宋体固定为正文常用的 400 字重；粗体由浏览器合成。
- 保留全部原有字符覆盖：宋体 30,928 个字符、行书 7,015 个、英文花体 1,081 个；没有通过删去生僻字来缩小体积。
- 保留全部五套封面与五套桌面原有显示资源，移除运行时不再使用的五张旧封面 JPG。所有在用图片保持原文件和画质。
- 安装包不再包含开发测试页面、截图和旧验证报告。
- 字体仍在插件内，不依赖外部字体网站；离线可用。

## 安装

覆盖原扩展目录内的文件，包含 `assets/fonts/*.woff2`，刷新后确认底部显示 **v0.23.1**。用户手札和聊天数据不在扩展文件夹中，本版保持相同的数据键。

如果此前已经安装 0.23.0，为收回服务器磁盘空间，可以在确认新版工作正常后删除原扩展内三个旧字体文件：`NotoSerifSC.ttf`、`ZhiMangXing-Regular.ttf`、`GreatVibes-Regular.ttf`。只覆盖安装也能正常工作，但 ZIP 不会自动删除服务器上已有的旧文件。

功能入口和使用说明见 [0.23.0 更新说明](更新说明-0.23.0.md)。字体来源与 OFL 许可证保持随包附带。

本版使用真实 Chromium / Edge 和酒馆接口替身验证字体加载、预览、长文滚动、截断续接及寄信流程。真实酒馆、用户模型账号及 Safari / iPhone 未实测。
