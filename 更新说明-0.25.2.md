# 0.25.2 自动整理漏触发修复

本次重点修复调度链路，不扩大日期正则去猜测时间。保留 0.25.1 的状态栏日期优先和“第二天”等正文识别。

## 已复现的原因与修正

- 结束事件早于正文写入时，旧版清除了待检查状态。现在等待消息写入，消息收到与渲染事件也会独立触发检查，不再必须先收到生成开始事件。
- 手动生成一个栏目和自动批量整理共用完成签名，导致整轮被跳过。现在批量整理有独立完成标记。
- 旧版先推进故事日，再请求生成；生成失败后跨日信息已被消费。现在先保存待整理区间，成功后清除；失败后保留，在下一条新回复到达时重试。重复事件不重试付费请求。
- 初次启用或旧数据没有时间基线时，会把新日期当作起点而漏掉第一次跨日。现在从此前的可见聊天恢复基准，不读取思维链，不调用模型回溯。
- 异步检查绑定聊天与插件实例，切换聊天后不再把旧回调应用到新聊天。加载期间多次检查也不会重复发送。

## 使用及限制

开启「按故事日自动整理」一次即可。正常回复完成后约 0.5 秒检查；仍在生成、等待存储加载或其他任务结束时会延后。

检测不消耗模型请求；真实跨日后沿用批量生成。网络失败的待办会在下一条新回复后重试一次，重复通知不会连续收费重试。待办持久化仍取决于浏览器存储或聊天元数据能否正常保存。

日记页脚会显示识别到的故事日、未识别到日期或失败待办等状态。排查时可在控制台运行 `window.__stPrivateJournalRuntime.autoDay()`，返回最后检查原因、日期与待办信息，不包含聊天正文。

仍仅支持消息原文中的状态栏，无法自动读取独立插件变量、动态 iframe 或图片里的日期。未取得用户实际状态栏样例，不能宣称已覆盖该环境的全部问题。

## 验证

- `node tests/auto-lifecycle.cjs`：10 个调度场景，包括事件乱序、缺少开始事件、手动生成冲突、失败保留/重试、历史基准、繁忙延后、聊天切换、加载竞态、关闭开关。
- `node tests/status-clock.cjs`：状态日期格式、思维链/隐藏内容排除、同日去重及跨日调度。
- `node tests/hidden-mail.cjs`、`node tests/version.cjs`：寄信及版本一致性回归。
- `node tests/concurrency-browser.cjs`：Chrome 390px/1280px，使用安装后的事件监听器、真实批量解析与保存代码，模拟模型返回；检查并发和字体旧功能，及跨日失败恢复。

测试不请求真实模型。没有真实 iPhone 或用户云酒馆验证。
