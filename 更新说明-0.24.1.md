# 私语手札 0.24.1

修正 0.24.0 压缩后的桌面清晰度和扩展设置分组。

- 五张桌面背景恢复到约 1536 × 1024 的原始尺寸。直接从压缩前的资源重新编码，不放大 768 像素旧图；保留画面细节，兼顾安装包体积。
- 扩展程序列表现在显示独立的“私语手札”折叠区。展开后可见“打开手札”和“API 设置”；收起后整个设置区一并隐藏，副 API 不再作为单独一条散落在其他扩展旁边。
- 在“跟随正文 API”模式下隐藏副 API 的连接和模型选项，切换回副 API 后仍保留选择。
- 背景使用新的资源文件名，脚本和样式更新缓存版本，避免覆盖文件后继续显示低清缓存图。

更新：将本包 `My-Dear-Memory-main` 内的文件覆盖到原插件目录，刷新酒馆；手札底部应显示 `v0.24.1`。无需卸载插件或清空数据。

本版沿用 0.24.0 的同日合页、正文分段、印象栏目、字体和五种寄信动画。没有修改生成、寄送和保存逻辑。

验证：真实 Chromium 浏览器中的 390px / 1280px 酒馆接口模拟检查，涵盖浅色手机主题、深色桌面主题、键盘展开、主副 API 切换、模型拉取、扩展抽屉重建、从设置打开手札，以及五张背景的实际加载尺寸。另检查 2469px 宽屏显示。真实酒馆主题和 iPhone Safari 未在本地实测。
