# 0.25.1 状态栏故事日期识别

- 优先识别聊天消息里的状态栏日期字段，例如 `日期：2025年9月25日`、`当前时间：2025-09-25 07:45`，以及 Markdown 表格、HTML 表格、`<status>` 和 `<statusbar>` 内的日期。
- 没有有效状态日期时，保留原来的正文日期及“第二天、次日、一个月后”等叙事跨日识别；同时读取本轮 User 输入和 Char 回复。
- 第一次状态日期建立基准。日期向后推进才触发整理，同一天不重复，倒退日期不自动整理。相对跨日之后出现同一天的明确日期，不重复触发。
- 继续排除独立思维字段、思维标签、HTML 注释、脚本、样式和常见预约日期字段。
- 只有 `07:45` 等时分、不含年月日时不会推测跨日，也不读取酒馆消息上的现实发送时间。

需要开启「按故事日自动整理」。日期检测本身不调用模型，识别跨日后沿用一次批量整理（截断时仍可能追加补全请求）。

兼容范围是保存在聊天消息原文中的状态栏。独立插件变量、动态 iframe、图片状态栏不在本次自动读取范围；特殊格式需提供实际消息原文后适配。没有尝试扫描整个网页，以免把现实日期、历史消息或预约日期当作当前故事日期。

测试：`node tests/status-clock.cjs` 验证常见格式、误识别排除、相对跨日和自动整理调度；另保留版本一致性、寄信及并发浏览器回归测试。未在用户实际状态栏和真实手机酒馆上测试。
