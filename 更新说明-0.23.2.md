# 0.23.2 轻量修正版

本版以缩小插件下载体积为优先，保留手札截断补全、日历信箱、全部五套封面与五套桌面。

## 体积与字体

- 完整中文字体文件不再放进插件主包。默认采用设备系统宋体，启动和默认打开手札不会请求在线字体。
- 英文花体 Great Vibes 约 158 KB，仍内置；系统宋体、系统黑体和英文花体无需下载外部字体。系统字体的具体样式取决于设备，英文花体中的中文使用系统字体。
- 需要统一的中文宋体或志莽行书时，在右上角“字体”选择标注“在线”的选项。首次选用时从 Google Fonts 获取字体样式，字体文件由浏览器按需下载并缓存；后续是否需要联网取决于浏览器缓存。若字体服务不可达，显示加载失败提示并暂用系统字体，可重试或改选离线字体。
- 只请求固定字体家族地址，不把手札正文、聊天内容或用户姓名写入字体请求 URL。没有使用 Google Fonts 的 `text=` 参数。加载状态检测使用的固定预览文本只在浏览器本地参与字体解码。
- 全部十张在用图片重新压缩，保留原分辨率、构图和透明边缘。图片采用有损压缩降低体积，已检查手机和桌面显示。

## 功能入口

- 右侧“日历信箱”：填写贺卡 / 情书，选择故事日期，或导入已保存的情书，然后预约、取消或立即寄出。
- 到达或跨过预约故事日期后，信件原文在本轮回复结束后加入当前聊天；下一轮角色回复可读到它。寄信不调用模型。首次识别需要正文中明确年月日；关闭酒馆期间不运行，重新打开聊天会补查到期信件。
- 手动生成输出预算为 5200 token，普通 / 跨阶段批量为 10000 / 24000 token。检测到结构截断时最多追加两次续接，完整响应仍只请求一次。失败保留已有文字，旧页面从未收到的结尾不会凭空还原。

## 安装

将本包 `My-Dear-Memory-main` 内的内容覆盖原扩展目录，包含 `assets`，刷新后确认底部为 **v0.23.2**。数据键保持不变，既有手札和寄信记录继续读取。

ZIP 不会自动删除服务器上的旧文件。如果之前装过 0.23.0 / 0.23.1，确认新版正常后，可以只删除旧的 `NotoSerifSC.ttf`、`ZhiMangXing-Regular.ttf`、`GreatVibes-Regular.ttf`、`NotoSerifSC.woff2`、`ZhiMangXing-Regular.woff2`，回收旧字库占用；请保留新版仍使用的 **GreatVibes-Regular.woff2**。无需删除聊天数据或清空手札。

## 验证

综合回归、寄信回归、8 组截断与日期回归通过。真实 Edge / Chromium 在 390×900 和 1280×900 下通过在线字体真实加载测试，以及模拟字体服务失败后的回退 / 重试测试；默认打开不下载在线中文字体。寄信流程、长文滚动和字体预览也通过。

实际酒馆、用户模型账号以及 Safari / iPhone 未实测。字体服务器连接在用户网络下可能不同。

字体来源：[Great Vibes](https://github.com/google/fonts/tree/main/ofl/greatvibes)、[Zhi Mang Xing](https://github.com/google/fonts/tree/main/ofl/zhimangxing)、[Noto Serif SC](https://github.com/google/fonts/tree/main/ofl/notoserifsc)。内置字体附带 OFL 许可，在线接口用法依据 [Google Fonts CSS API](https://developers.google.com/fonts/docs/css2)。
