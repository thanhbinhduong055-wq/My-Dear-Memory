# 0.25.0：分板块任务与精选在线字体

## 生成

- 副 API：印象、相处日记、情书、已解锁的恋爱日记可同时请求，互不锁住按钮。相同板块的重复点击不会追加请求。
- 正文 API：可以连续提交不同板块，插件按顺序执行。酒馆的 `generateQuietPrompt` 使用共享生成状态和临时字数设置，不适合直接并行调用。
- 页脚显示当前聊天的任务。失败只释放对应任务，其他任务继续执行。
- 各任务保存到发起时的手札；同一本手札的写入按顺序完成。后台任务期间重新打开手札不替换正在使用的内存数据。
- 每个手动生成任务通常各消耗一次请求；不是多个手动任务合计一次。原有按故事日批量整理仍通常一次请求，响应截断时最多追加两次补全。

## 五款新字体

保留原有选项，在手札内页顶部「字体」中增加：

| 风格 | 字体 / 官方收录页 | FontsAPI 家族名 |
| --- | --- | --- |
| 毛笔字 | [正风毛笔字体](https://fonts.zeoseven.com/items/700/) | MasaFont |
| 宝宝风手写 | [呆萌手写体](https://fonts.zeoseven.com/items/638/) | DymonShouXieTi |
| 文艺楷书 | [霞鹜文楷](https://fonts.zeoseven.com/items/292/) | LXGW WenKai |
| 随性手写 | [悠哉字体](https://fonts.zeoseven.com/items/192/) | Yozai |
| 清瘦仿宋 | [朱雀仿宋](https://fonts.zeoseven.com/items/7/) | Zhuque Fangsong (technical preview) |

五款收录页均标注 OFL-1.1。本插件链接到 ZeoSeven 公益 FontsAPI，不重新分发这些字体文件；字体版权归各作者，许可见各收录页及其上游链接。

注意：朱雀是仿宋，**不是瘦金体**。目前未确认字体库内有许可明确的免费瘦金体，暂提供这款清瘦风格候选，后续可按用户确认替换。

只加载选中的字体，不预下载完整字体库；首次需要网络。加载失败时保持系统字体，可点击重试，不影响读写。请求不含日记正文，但字体服务仍会收到常规网络信息（例如 IP 和字体资源路径）。缺失字形回退到设备字体。

## 验证方式

- `node --check index.js`
- `node tests/version.cjs`
- `node tests/hidden-mail.cjs`
- 使用已有 Playwright 安装：`node tests/concurrency-browser.cjs`。可通过 `PJ_BROWSER` 指定 Chrome 路径。

浏览器测试使用模拟酒馆环境和模拟模型返回，不调用真实模型，不消耗模型额度。覆盖 390px / 1280px，不能代替真实 iOS Safari 或用户云酒馆的实机验证。

运行版本可在手札页脚确认：`v0.25.0`。脚本、样式标记和 manifest 缓存参数同步更新。
