# 0.23.3 — 隐藏寄信正文，核对故事年份

- 新寄出的情书/贺卡只显示“✉ 一封情书已寄出。”或“✉ 一张贺卡已寄出。”，完整投递内容使用标准 `<!-- Request: … -->` 注释保存在聊天消息原文中。自动到期寄送与手动寄送使用相同格式。
- 信件中可能终止注释的字符会转义；日历信箱保留未改写的原文。已发送的旧聊天消息不会自动改写。
- 日历默认跟随已识别的故事年月日。手动选定的预约日期不会被后台日期检查覆盖；“使用故事当天”可重新定位到故事日期。
- 预约年份与故事年份不同时会明确提示，包括已有的待寄信件。跨年预约仍然允许，不会擅自提前寄送。
- 寄送依据故事日期，而不是现实日期。例如故事是 2025-09-25、预约是 2026-09-25，信件将继续等待。若想在故事当天寄送，请取消错误预约后按正确年份重新预约，或使用“现在寄出”。

## 使用边界

寄信不调用模型、不自动生成下一轮剧情。信件在本轮正文结束后加入聊天，下一次正文请求可读取信件并描写回应。

HTML 注释只是界面隐藏，不是加密；编辑消息、导出聊天、查看原始请求仍可看到信件。若酒馆预设或第三方正则会删除发送给模型的 HTML 注释，需要保留这些 Request 注释，否则模型无法读取内容。实际角色是否回应仍受预设、上下文与模型输出影响。

尚未识别完整故事日期时，会提示核对日期，不会依现实时间自动寄信。此版未扩大日期识别格式或改写历史预约。

## 验证范围

- 可运行 `node tests/hidden-mail.cjs` 检查截图中的跨年预约、到期寄送、Request 注释边界字符、原信件保留、重复检查、失败重试、故事日期默认值、手动选择保护、跨聊天切换以及寄信零模型请求。
- 已在真实 Chrome 的 390px 手机模拟视口与 1280px 桌面视口，配合本地 SillyTavern 接口模拟环境验证表单操作和 HTML 注释隐藏效果。没有访问真实模型，也没有在用户的云酒馆、iOS Safari 或第三方聊天美化/正则配置中实测。
